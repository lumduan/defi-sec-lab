# ---------------------------------------------------------------------------
# Secrets & key material: must never be committed (AGENTS.md hard rule 2)
# ---------------------------------------------------------------------------
.env
.env.*
!.env.example
*.key
*.pem
*.p12
*.pfx
*.keystore
keystore/
keystores/
CLAUDE.local.md
# (.secrets.baseline is intentionally TRACKED: detect-secrets' audited baseline of known non-secrets)

# ---------------------------------------------------------------------------
# Foundry build artifacts & caches
# ---------------------------------------------------------------------------
out/
cache/
# forge script run records (this lab never uses --broadcast; ignored regardless)
broadcast/

# ---------------------------------------------------------------------------
# anvil state dumps (large; regenerate from fork.env instead)
# ---------------------------------------------------------------------------
state/
*.state.json

# ---------------------------------------------------------------------------
# learn/ app build artifacts (Next.js web, FastAPI api)
# ---------------------------------------------------------------------------
node_modules/
.next/
*.tsbuildinfo
next-env.d.ts
npm-debug.log*
__pycache__/
*.py[cod]
.venv/
.pytest_cache/
.ruff_cache/
.mypy_cache/
.coverage
coverage/
htmlcov/

# ---------------------------------------------------------------------------
# OS / editor
# ---------------------------------------------------------------------------
.DS_Store
.idea/
.vscode/

"""defi-sec-lab inspector.

Re-runs the read-only inspection of Aave V3's native-USDC reserve on Arbitrum One (notes/00-first-fork.md)
and returns every step as decoded JSON, raw values first.
"""

SCHEMA = "defi-sec-lab/inspection@1"
